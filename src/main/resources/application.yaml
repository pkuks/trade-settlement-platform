security:
  jwt:
    secret-key: 4f2d5f9e7c8a6b3d1e4f5a7c9d2e6f8b4a1c3d5e7f9a2b6c8d1e4f5a7c9d2e6
    expiration-ms: 3600000

spring:
  application:
    name: trade-settlement-platform

  datasource:
    url: jdbc:postgresql://localhost:5432/settlement_db
    username: settlement_user
    password: settlement_password

  jpa:
    hibernate:
      ddl-auto: validate

    show-sql: true

  flyway:
    enabled: true

#  autoconfigure:
#    exclude:
#      - org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
#      - org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration